# Required: API key that clients must present as Bearer token
PROXY_API_KEY=change-me-to-a-strong-random-key

# Optional: port to listen on (default 3000)
PORT=3000

# Optional: data directory for persistent state (default /data)
DATA_DIR=/data

# Optional: log level (default info)
LOG_LEVEL=info
